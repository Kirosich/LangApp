services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - server/.env
    volumes:
      - ./data:/app/data
      - /home/deploy/backups/langapp:/app/backups
    expose:
      - "3000"
