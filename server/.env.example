PORT=3000
AUTH_USER=changeme
AUTH_PASS=changeme

# Optional -- Telegram bot (reminders + stats). Leave both unset and the
# app runs normally with the bot disabled (one warning logged at start).
# See README "Telegram-бот" for how to create the bot and find your chat_id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
