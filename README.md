# Langapp

Личное приложение для изучения казахского (A2) и английского (B2) через
карточки со spaced repetition (FSRS), квизы и отдельный раздел теории
(грамматика). Живёт на эфиргейм.рф, вместе с побочным проектом того же
домена.

## Функции

**Карточки и повторение**
- SRS по FSRS (`server/srs/fsrs.js`, обёртка над `ts-fsrs`) — планирование
  на модели памяти (стабильность/сложность/вероятность вспомнить), тот же
  алгоритм, что в Anki по умолчанию. Мигрировано с SM-2 (`server/srs/sm2.js`,
  старые SM-2-поля `progress` оставлены в схеме для сравнения/отката, но
  больше не обновляются); `review_log` — сырая история повторений для
  будущей персонализации весов алгоритма
- Пять типов квиза: multiple choice, typing (толерантность к опечаткам,
  Левенштейн ≤1), matching pairs, «Собери предложение» (тапом по словам
  восстановить `example_sentence` карточки; только active/mastered,
  не backlog; пунктуация приклеена к словам, не отдельные фишки),
  «Аудирование» (термин озвучивается через TTS вместо показа текста;
  вопросы для языка без голоса на устройстве отфильтровываются сами —
  без битых беззвучных карточек; своя строка точности, отдельно от
  остальных квизов)
- «Тренировка» — свободный прогон карточек (флип-карты без оценок) без
  влияния на SRS-расписание: либо как бонус-раунд после обычной сессии
  повторения, либо отдельным входом с дашборда на заданное число слов
  (по умолчанию 100), зацикливается с новой перетасовкой после каждого
  прохода
- «Знаю досконально» — пометка карточки как mastered, чтобы больше не
  показывалась ни в SRS, ни в квизах (`/api/cards/:id/master`)
- Склад (backlog): новые карточки не сваливаются в SRS все разом, а
  вводятся дозированно по дням (`server/backlog/introduce.js`),
  с настройкой темпа и ручным «бустом» на дашборде
- Озвучка произношения (🔊) на карточке в Study, Browse и «Мои знания» —
  через браузерный Web Speech API (`client/src/utils/tts.js`), без
  бэкенда и файлов; кнопка сама скрывается, если у устройства нет голоса
  для нужного языка (`kk-KZ` для казахского, `en-US` для английского) —
  на iOS (Safari и Chrome, общий движок WebKit) казахского голоса нет
  вообще, это платформенное ограничение, а не выборочный баг
- CEFR-уровень (A1/A2/B1/B2/C1) на каждой карточке (`cards.level`) —
  проставлен вручную по фактической сложности слова, а не механическим
  переносом темы в уровень; пока только данные, фильтр по уровню в UI
  не подключён
- PWA: устанавливается на экран (manifest + иконки), мгновенная загрузка
  за счёт service worker (`vite-plugin-pwa`). Кэшируется только статика
  (shell/JS/CSS/иконки) — API всегда идёт в сеть напрямую, ничего не
  precache-ится заранее (весь сервер за Basic Auth, precache-фетч без
  логина просто получил бы 401). При пропаже сети — баннер «Нет сети»
  вместо белого экрана

**Теория (грамматика)**
- Отдельный раздел параллельно вокабуляру: курс → блоки (план/факт
  минуты, статус) → пункты-чеклист с заметками
  (`server/routes/theory.js`, `client/src/pages/Theory*.jsx`)
- Плюс независимый мини-справочник теоретических тем со своим прогрессом
  прочтения (`server/routes/theoryReference.js`)
- Опциональная связка тема карточек ↔ slug темы справочника
  (`theory_theme_links`, `server/seed-theory-links.js`): на карточке в
  Study/Browse — ссылка «Теория» на связанную тему; на странице темы
  справочника — блок «Практика» (выучено/всего) и кнопка «Учить эти
  слова» с фильтром по теме
- Грамматические дриллы на странице темы (`theory_drills`,
  `server/seed-theory-drills.js`, кнопка «Начать дриллы»): multiple
  choice, где неправильные варианты — реальные грамматические ошибки
  (не тот вариант гармонии/звонкости суффикса), а не случайный шум;
  100 дриллов на 20 тем (12 KZ + 8 EN). Трекается отдельным
  `session_type = theory_drill`, точность **не** подмешивается в общий
  график точности квизов — грамматика и лексика разные навыки
- Сценарные диалоги (Теория → вкладка «Диалоги»): базар/такси/кафе,
  каждая реплика с озвучкой (🔊) и переводом. Построены преимущественно
  из уже существующих в колоде слов (бытовые фразы, покупки-и-деньги,
  транспорт) — не вывал новой лексики; немногочисленные новые слова на
  диалог показаны отдельным блоком «Новые слова», а не молча подмешаны
- Раздел чтения (Теория → вкладка «Чтение»): тексты с уровнем/темой/
  стилем (учебный — плотно завязан на лексику колоды; жанровый —
  аутентичный регистр, сказки/статьи) + упражнения на понимание после
  каждого текста, оценка server-side. Весь контент оригинальный
- Экзамен на уровень (Теория → вкладка «Экзамен»): до 50 случайных
  вопросов из накопленного пула дриллов уровня, нужны все 100%
  правильных — частичный зачёт не считается. Полная история попыток
  (`level_exam_attempts`)
- Координационная сводка (`GET /api/coaching/summary`): слабые/сильные
  места по каждому языку в одном месте, для ручных запросов советов в
  чате — не встроенная AI-интеграция

**Геймификация и статистика**
- XP и уровни за повторения/квизы/мастеринг (`server/xp/`)
- Бейджи, личные рекорды, streak (дней подряд с завершённой сессией)
- Графики: heatmap активности за 90 дней, накопительный прогресс,
  разбивка по темам, тренд точности квизов, проблемные карточки,
  недельный recap, время в занятиях (сегодня/неделя/всё время)
- Учёт сессий (`study_sessions`) — время и количество карточек, отдельно
  для study/quiz/theory_drill/level_exam/reading, и отдельно по языку
  (kz/en) через `minutes_by_language` в `/api/stats`
- Квесты на дашборде (`server/routes/quests.js`): 3 дневных + 3 недельных
  цели (карточки, минуты, дни подряд, новые слова), считаются на лету из
  тех же таблиц/агрегатов, что и остальная статистика — отдельного
  трекинга прогресса квестов нет, никакого нового источника правды
- Умный слой: leech-детект (проблемные карточки — EF ≤ 2.0 после хотя бы
  одного повторения) с реальными действиями — переписать пример
  (ссылка на редактирование) или вернуть в склад со сбросом прогресса
  (`POST /api/cards/:id/demote`). «🎯 Тренировка дня» одной кнопкой на
  дашборде — `GET /api/cards/workout` собирает сессию из лечей и due-карточек
  (без дублирования их логики выборки) и открывает Study с готовым списком id
- Telegram-бот (опционально): напоминания, `/stats`, `/due`, недельный
  recap, уведомления об уровне/бейджах — подробности в разделе
  «Telegram-бот» ниже

## Стек

- **Backend**: Node.js + Express, SQLite (`better-sqlite3`), Basic Auth
- **Frontend**: Vite + React + Tailwind CSS, тёмная тема, мобильный-дружелюбный
- **Деплой**: Docker + docker-compose, GitHub Actions; TLS и проксирование
  на хосте — системный nginx (общий для всех сайтов на сервере)

## Структура проекта

```
langapp/
├── server/           # Express API + SQLite + FSRS
│   ├── db/           # подключение к БД и миграции (выполняются при старте)
│   ├── srs/          # fsrs.js — обёртка над ts-fsrs; sm2.js — старый алгоритм, оставлен для сравнения
│   ├── xp/           # расчёт XP и уровней
│   ├── gamification/ # определения бейджей, хук на конец сессии
│   ├── backlog/       # дозированное введение новых карточек
│   ├── telegram/      # бот: long polling, команды, напоминания (опционально)
│   ├── routes/        # cards, quiz, stats, sessions, theory,
│   │                   # theoryReference, gamification, backlog
│   ├── middleware/    # Basic Auth
│   ├── __tests__/     # unit-тесты (vitest)
│   ├── seed.js                  # стартовые карточки
│   ├── seed-theory*.js          # курсы теории (казахский/английский)
│   └── seed-vocab-advanced.js   # расширенная лексика
├── client/           # React + Tailwind SPA
│   └── src/
│       ├── pages/        # Dashboard, Study, Quiz, Browse, AddCard,
│       │                 # KnownWords, Settings, Theory*, Login
│       ├── components/   # Layout, компоненты квизов, progress/ (графики)
│       ├── api/          # обёртка над fetch с Basic Auth
│       ├── hooks/        # useStudySession — трекинг времени сессии
│       └── context/      # AuthContext
├── data/             # SQLite-файл (app.db), не в git
├── scripts/
│   └── backup-db.sh  # хостовый враппер для cron — ночной бэкап БД
├── Dockerfile        # multi-stage: сборка client -> прод-образ с API
├── docker-compose.yml
└── .github/workflows/deploy.yml
```

## Локальный запуск

### 1. Backend

```bash
cd server
npm install
cp .env.example .env   # при желании поменяйте логин/пароль
npm run seed           # один раз — заполнит БД стартовыми карточками
npm run dev            # http://localhost:3000
```

### 2. Frontend

```bash
cd client
npm install
npm run dev             # http://localhost:5173, проксирует /api на localhost:3000
```

Откройте `http://localhost:5173`, войдите с логином/паролем из `server/.env`.

### Тесты

```bash
cd server
npm test                # unit-тесты FSRS/SM-2 и других чистых модулей
```

## Сборка и деплой через Docker Compose

1. На сервере склонируйте репозиторий и создайте `server/.env` (см.
   `server/.env.example`) с реальными логином/паролем.
2. Запустите:

   ```bash
   docker compose up -d --build
   ```

   Сервис `app` соберёт client и поднимет Express на порту 3000 внутри
   контейнера, опубликованном на хосте только на `127.0.0.1:3001`
   (см. `docker-compose.override.yml`) — данные SQLite хранятся в `./data`,
   смонтированной volume.
3. TLS и публичный доступ по домену — через системный **nginx** на хосте
   (не в докере): для каждого сайта на сервере, включая langapp, у него
   свой vhost с сертификатом Let's Encrypt (certbot), который проксирует
   на `127.0.0.1:<порт контейнера>`. Заводить домен для langapp — значит
   добавить свой vhost-файл в `/etc/nginx/sites-available/`, как у
   остальных сайтов на машине; отдельный reverse-proxy контейнер (Caddy
   и т.п.) здесь не нужен и раньше конфликтовал за порты 80/443 с этим же
   nginx (см. «Обновления» ниже).

## Бэкапы БД

Ежедневно в 04:00 по времени сервера (cron пользователя `deploy` на
хосте) запускается `scripts/backup-db.sh` → внутри контейнера
`server/scripts/backup-db.js` снимает атомарный снапшот `data/app.db`
через `better-sqlite3`'s `.backup()` (SQLite Online Backup API — безопасно
на «горячую» базу, в отличие от простого `cp`), проверяет, что снимок
читается, и хранит последние 14 копий в `/home/deploy/backups/langapp/`
(вне репозитория и вне `data/`, на хосте).

Ручной запуск / восстановление:

```bash
./scripts/backup-db.sh                          # снять бэкап вручную
docker exec langapp-app-1 node -e "              # проверить снимок
  const Database = require('better-sqlite3');
  const db = new Database('/app/backups/app-2026-08-20.db', { readonly: true });
  console.log(db.prepare('SELECT COUNT(*) FROM cards').get());
"
# восстановление — остановить контейнер, подменить data/app.db файлом
# из /home/deploy/backups/langapp/, запустить контейнер снова
```

## Telegram-бот

Личный бот с напоминаниями и статистикой (`server/telegram/`). Отвечает
**только** настроенному `TELEGRAM_CHAT_ID`, остальных молча игнорирует.
Если переменные не заданы — бот просто не стартует (одно предупреждение
в лог), само приложение работает как обычно. Long polling, без вебхука —
публичный HTTPS-эндпоинт для бота не нужен.

**Настройка:**

1. Напишите **@BotFather** в Telegram → `/newbot` → следуйте подсказкам →
   получите токен вида `123456789:AAExampleTokenAbcDef...`.
2. Напишите своему новому боту любое сообщение (иначе `getUpdates` ничего
   не вернёт).
3. Узнайте свой `chat_id`: откройте в браузере
   `https://api.telegram.org/bot<ТОКЕН>/getUpdates` и найдите
   `"chat":{"id":...}` в ответе — это и есть `TELEGRAM_CHAT_ID`.
4. Впишите оба значения в `server/.env` на сервере (см.
   `server/.env.example`) и перезапустите контейнер
   (`docker compose up -d --build`).

**Что умеет:**

- `/stats` — уровень, XP, streak, слов выучено по языкам, время сегодня/за неделю
- `/due` — сколько карточек к повторению сейчас + сколько новых введётся из склада
- `/help` — список команд
- Вечернее напоминание в 19:00 по Алматы — только если сегодня (по
  Алматы) не было ни одной завершённой сессии
- Недельный recap по понедельникам в 10:00 по Алматы
- Уведомление при новом уровне и новом бейдже — хук в существующую
  логику начисления XP (`cards.js` review/master, `theoryReference.js`
  read, `sessions.js` end), без параллельного пересчёта

Вся логика времени бота — часовой пояс `Asia/Almaty` (`server/telegram/almatyTime.js`,
через `Intl`, без доп. библиотек и без хардкода смещения). Это отдельно
от остального приложения, где «сегодня» по-прежнему считается по UTC
серверного времени (не меняли, чтобы не задеть существующую логику SRS/streak).

## Автодеплой (GitHub Actions)

`.github/workflows/deploy.yml` запускается при каждом push в `master`:
подключается по SSH к серверу и выполняет `git pull && docker compose
up -d --build`. Для работы нужно завести 3 секрета в **Settings →
Secrets and variables → Actions**:

- `DEPLOY_SSH_KEY` — приватный SSH-ключ для деплоя
- `SSH_HOST` — адрес сервера
- `SSH_USER` — пользователь на сервере

## API

| Метод | Путь | Описание |
|---|---|---|
| GET | `/api/cards/due?language=&theme=` | карточки к повторению сегодня (SM-2), опционально по теме |
| GET | `/api/cards/workout?language=&limit=` | «Тренировка дня» — id карточек: сначала лечи, потом due |
| GET | `/api/cards?theme=&language=&ids=&status=` | список/фильтр карточек (`status`: active/backlog/mastered) |
| GET | `/api/cards/known?language=` | освоенные карточки |
| POST | `/api/cards` | добавить карточку |
| POST | `/api/cards/import` | массовый импорт карточек |
| PUT | `/api/cards/:id` | редактировать карточку |
| DELETE | `/api/cards/:id` | удалить карточку |
| POST | `/api/cards/:id/review` | отправить оценку (0–5), обновить SM-2, начислить XP |
| POST | `/api/cards/:id/master` / `/unmaster` | пометить как «уже знаю» / вернуть в оборот |
| POST | `/api/cards/:id/demote` | отправить карточку обратно в склад, сбросив SRS-прогресс (для лечей) |
| GET | `/api/quiz?type=choice\|typing\|matching\|sentence\|listening&theme=&language=&count=` | сгенерировать квиз |
| GET | `/api/stats?language=` | всего карточек, к повторению, streak, по темам |
| POST | `/api/sessions/start` / `POST /api/sessions/:id/end` | трекинг длительности сессии study/quiz |
| GET/POST/PUT/DELETE | `/api/theory/courses`, `/blocks`, `/items` | курсы теории → блоки → пункты-чеклист |
| GET | `/api/theory?language=` / `/api/theory/:slug` | мини-справочник теоретических тем |
| POST | `/api/theory/:slug/read` | отметить тему прочитанной, начислить XP |
| GET | `/api/theory/theme-links?language=` | связки тема карточек ↔ slug темы справочника |
| GET | `/api/theory/:slug/drills` | грамматические дриллы темы (multiple choice) |
| GET | `/api/dialogues?language=` / `/api/dialogues/:slug` | сценарные диалоги (базар/такси/кафе) |
| POST | `/api/dialogues/:slug/read` | отметить диалог прочитанным, начислить XP |
| GET | `/api/quests` | дневные/недельные квесты, посчитаны на лету из существующей статистики |
| GET | `/api/gamification/summary` \| `/badges` \| `/heatmap` \| `/cumulative` \| `/topics-breakdown` \| `/accuracy-trend` \| `/listening-accuracy-trend` \| `/problem-cards` \| `/milestones` \| `/weekly-recap` | статистика и геймификация для дашборда |
| GET | `/api/backlog/summary` \| `/settings` | состояние и настройки склада новых карточек |
| PUT | `/api/backlog/settings` | темп ввода новых карточек в день |
| POST | `/api/backlog/boost` | разово ввести N карточек из склада сверх нормы |

Все запросы требуют Basic Auth (логин/пароль из `.env`).

## Обновления

Ключевые изменения по датам — веду по мере доработки приложения.

- **2026-08-20** — режим свободной «Тренировки»: вход с дашборда на
  заданное число случайных карточек (по умолчанию 100), без влияния на
  SM-2-расписание; отдельно от бонус-раунда после обычной сессии.
- **2026-08-20** — исправлен «прыгающий» нижний навбар на мобильном
  Safari (app-shell layout вместо fixed-позиционирования).
- **2026-08-20** — расширенная лексика: 35 английских слов (B2–C1) и 40
  казахских (A1–A2) по темам; склад новых карточек и система
  mastered/«уже знаю».
- **2026-08-20** — автодеплой через GitHub Actions при push в `master`.
- **2026-08-20** — убран Docker Caddy: он занимал хостовые порты 80/443
  и клал доступ ко всем остальным сайтам сервера. Системный nginx уже
  умеет всё нужное сам (свой vhost с Let's Encrypt для домена langapp,
  проксирует на `127.0.0.1:3001`) — Caddy оказался лишним и конфликтующим
  слоем, полностью убран из `docker-compose.yml`.
- **2026-08-20** — автоматические ночные бэкапы БД: `scripts/backup-db.sh`
  через cron (04:00 по времени сервера) вызывает `server/scripts/backup-db.js`
  внутри контейнера — атомарный снимок через `better-sqlite3`'s `.backup()`
  (тот же SQLite Online Backup API, что и CLI `.backup`), хранит последние
  14 снимков в `/home/deploy/backups/langapp/`, каждый раз проверяет, что
  бэкап реально читается.
- **2026-08-20** — склад пополнен на 300 карточек (Этап 2 плана): 150
  казахских (A1–A2: одежда, погода, дни недели и месяцы, глаголы движения,
  вопросительные слова, здоровье, покупки и деньги, качества) и 150
  английских (B2–C1: связки речи, устойчивые сочетания make/do/take/have,
  карьера, точные глаголы вместо общих, аргументация). Все — в `backlog`,
  вводятся дозированно; см. `server/seed-vocab-stage2-backlog.js`.
- **2026-08-20** — связка теории и карточек (Этап 3 плана): опциональная
  таблица `theory_theme_links` (тема ↔ slug темы справочника), ссылка
  «Теория» на карточках в Study/Browse и блок «Практика» (выучено/всего
  + «Учить эти слова» с фильтром по теме) на странице темы справочника.
  Пока связано 7 тем: `глаголы`/`глаголы движения` → времена глагола,
  `вопросительные слова` → вопросительные частицы, `семья` → аффиксы
  принадлежности, `покупки и деньги` → табыс септік; `карьера` → phrasal
  verbs про работу, `синонимы` → путаемые пары слов.
- **2026-08-20** — озвучка произношения (Этап 4 плана): кнопка 🔊 на
  карточке в Study/Browse/«Мои знания» через браузерный Web Speech API
  (`speechSynthesis`), без бэкенда — вариант 1 из плана. Фича-детект
  голоса на клиенте (`client/src/utils/tts.js`, `useSpeech`): кнопка
  показывается только если у браузера/ОС реально есть голос под `kk-KZ`
  или `en-US`, иначе молча скрывается. Требует проверки на реальном
  телефоне — казахский голос под Kazakh SRS есть не на всех платформах.
- **2026-08-21** — режим квиза «Собери предложение» (Этап 5 плана): новый
  тип на основе `example_sentence` карточек — слова перемешаны, тапом
  восстанавливаешь порядок, подсветка верно/неверно и показ перевода
  после ответа. Только карточки не из склада (active/mastered). Новый
  `session_type = quiz_sentence` (потребовал пересоздания `study_sessions`
  из-за CHECK-constraint в SQLite — данные сессий сохранены). Вход — как
  через выбор типа квиза, так и отдельной кнопкой на дашборде.
- **2026-08-21** — PWA (Этап 6 плана): `vite-plugin-pwa`, манифест +
  иконки 192/512 (`client/public/pwa-icon.svg` → `pwa-192.png`/`pwa-512.png`,
  сгенерированы через `rsvg-convert`), meta-теги для iOS
  (`apple-mobile-web-app-*`) — можно добавить на домашний экран.
  Service worker кэширует только shell/статику (`NetworkFirst`/
  `StaleWhileRevalidate`, только 200-ответы), без precache-манифеста и
  без кэша `/api/**` — сервер целиком за Basic Auth, поэтому precache
  «вслепую» до логина был бы небезопасен/бессмысленен. Полноценный
  офлайн с очередью синхронизации сознательно не делали — только
  мгновенная загрузка shell + баннер «Нет сети» вместо белого экрана.
- **2026-08-21** — Telegram-бот (Этап 7 плана, финал первой очереди):
  `server/telegram/` — long polling, отвечает только своему `chat_id`,
  `/stats`/`/due`/`/help`, вечернее напоминание в 19:00 и недельный recap
  по понедельникам в 10:00 (`Asia/Almaty`, через `Intl`, без хардкода
  смещения — проверено на границе суток 23:50), уведомления об
  уровне/бейджах через хук в существующую XP-логику. Без токена в `.env`
  бот просто не стартует, приложение не затронуто.
- **2026-08-21** — грамматические дриллы (Этап 8, начало второй очереди
  плана): `theory_drills` — multiple choice на странице темы справочника,
  100 дриллов на 20 тем (12 казахских: все 7 падежей, личные/притяжательные
  аффиксы, времена глагола, отрицание, вопросительные частицы, послелоги;
  8 английских). Дистракторы — реальные грамматические ошибки того же
  суффикса/семейства (например «-ке» вместо «-ге»), не случайные слова.
  Новый `session_type = theory_drill` (снова потребовал пересоздания
  `study_sessions`); точность дриллов намеренно не подмешивается в общий
  график точности квизов — грамматика и лексика разные навыки.
- **2026-08-21** — аудирование (Этап 9 второй очереди): новый тип квиза
  «Аудирование» поверх Web Speech API из Этапа 4 — термин озвучивается,
  текст скрыт до ответа. Переиспользует ту же выборку/генерацию вопросов,
  что и multiple choice (не дублировал расчёты) — разница чисто на
  клиенте. Вопросы для языка без TTS-голоса на устройстве отфильтровываются
  автоматически; если голоса нет вообще — понятное сообщение вместо
  немой викторины. Новый `session_type = quiz_listening`; точность —
  отдельный график `/api/gamification/listening-accuracy-trend`,
  специально не смешан с точностью остальных квизов (слышать и видеть
  слово — разные навыки).
- **2026-08-21** — сценарные диалоги (Этап 10 второй очереди): базар/такси/кафе
  на казахском, каждая реплика озвучена (переиспользует `SpeakButton` из
  Этапа 4) и переведена. Собраны преимущественно из слов, уже
  существующих в колоде карточек (проверил по реальному списку карточек
  перед тем, как писать реплики) — реализация закрепления, а не способ
  вывалить новую лексику; штучные новые слова на диалог вынесены в явный
  блок «Новые слова», не спрятаны внутри реплик. Раздел — новая вкладка
  «Диалоги» в Теории, чтение отмечается и даёт XP как и справочник.
- **2026-08-21** — квесты (Этап 11 второй очереди): виджет на дашборде с
  3 дневными (карточки, минуты, хотя бы одно занятие) и 3 недельными
  (дни подряд, карточки, новые слова) целями. Никакого параллельного
  трекинга — `GET /api/quests` считает прогресс каждый раз заново из
  `study_sessions`/`cards`/`xp_events`, переиспользуя уже существующие
  хелперы (`weekTotals`, `sumMinutes` из gamification.js/stats.js), а не
  копии их логики.
- **2026-08-21** — умный слой, финал второй очереди (Этап 12): у
  «проблемных карточек» (leech, EF ≤ 2.0) появились реальные действия —
  «Переписать пример» (в редактор) и «В склад» (`POST /api/cards/:id/demote`,
  сброс прогресса, повторное дозированное введение позже). Кнопка
  «🎯 Тренировка дня» на дашборде (`GET /api/cards/workout`) сама собирает
  сбалансированную сессию: сначала лечи, потом due-карточки, без дублей —
  переиспользует тот же порог лечей, что и виджет проблемных карточек.
  Этим второй пакет из 5 этапов (8–12) закрыт полностью.
- **2026-08-21** — миграция SRS-движка SM-2 → FSRS, поэтапно с чекпоинтами
  (`ts-fsrs`, проверенная реализация, не написана с нуля):
  - **A** — 10 новых полей `progress` (`fsrs_stability/difficulty/state/due`
    + служебные `reps/lapses/learning_steps/elapsed_days/scheduled_days/last_review`,
    нужные библиотеке для точного round-trip между сессиями), новая
    `review_log` (сырая история для будущей персонализации весов) и
    `fsrs_settings` (`request_retention`, по умолчанию 0.9, только в БД)
  - **B** — `server/srs/fsrs.js`, чистый модуль без обращения к БД:
    маппинг существующих кнопок оценки (1/3/4/5) на грейды FSRS, обёртка
    над `ts-fsrs`. 12 тестов, включая проверку «карточка после round-trip
    через колонки БД планируется идентично карточке в памяти» — самое
    важное для следующих этапов
  - **C** — миграция 46 активных карточек с реальным прогрессом: стабильность
    ≈ старый интервал, сложность — линейная карта от easiness_factor,
    due-дата сохранена как есть (расписание не сброшено). Backlog/mastered
    не тронуты — получат FSRS-состояние сами при первом повторении
  - **D** — переключение: `/review` теперь считает через FSRS и пишет
    в `review_log`; везде, где читался `due_date`, теперь читается
    `fsrs_due`. Отдельно расширил `LEARNED_CONDITION_SQL` и leech-детект
    FSRS-сигналом (старые SM-2-поля перестают обновляться и «замерзают» —
    без этого счётчики выученных слов, бейджи и вехи молча перестали бы
    расти для всего, что происходит после переключения)

  Каждый этап — бэкап БД перед изменением, отдельный коммит, проверка на
  живых данных (389 карточек до/после, идемпотентность миграции, полный
  цикл Again/Hard/Good/Easy на живом API). Старые SM-2-поля остаются в
  схеме некоторое время для сравнения, прежде чем их удалять отдельным
  шагом.
- **2026-08-21** — исправлена гонка в определении голоса TTS: промис
  голосов резолвился по первому непустому `getVoices()`, а часть
  браузеров (Android Chrome) сначала синхронно отдаёт маленький
  дефолтный набор и только потом асинхронно — полный список, включая
  редкие языки вроде `kk-KZ`. Теперь всегда ждём `voiceschanged` (с
  коротким запасным таймаутом) перед тем как решить, есть голос или
  нет. Диагностировано на реальном iPhone через добавленный on-screen
  дебаг (`?ttsdebug=1`, без Web Inspector): гонка не при чём — на iOS
  казахского голоса нет вообще ни в одном браузере (общий движок
  WebKit), кнопка 🔊 корректно скрывается по дизайну.
- **2026-08-21** — CEFR-уровни (A1/A2/B1/B2/C1) на всех карточках:
  колонка `cards.level`, проставлена вручную для всех 389 существующих
  карточек (`server/backfill-levels.js`) по фактической сложности
  слова, а не штампом темы целиком.
- **2026-08-21** — рост казахского словаря на 246 слов (склад): закрыты
  критические пробелы в базовой лексике (еда и числа/время были почти
  пустыми — только «вода» и «один»), расширены дом/профессии/тело/погода,
  добавлены новые темы природа, путешествия, технологии и интернет,
  работа и учёба (углублённо), общество и мнения (первая по-настоящему
  B1 тема). Итого 443 казахские карточки (A1=218, A2=164, B1=61).
- **2026-08-21** — рост английского словаря на 183 слова (склад),
  без новых A1/A2 — только B2/C1 с точечными B1-мостиками: устойчивые
  метафоры и образные выражения, эмоциональный интеллект и психология
  отношений, академическая/научная лексика, лексика соцсетей и цифровой
  коммуникации, разговорные усилители и хеджирование. Итого 375
  английских карточек (B1=42, B2=208, C1=118, плюс 7 старых A1
  сид-карточек).
- **2026-08-21** — координационная сводка `GET /api/coaching/summary`:
  слабые/сильные места по каждому языку (уровни, темы, проблемные
  карточки) в одном месте — для ручных запросов советов в чате, не
  автоматическая AI-интеграция.
- **2026-08-21** — раздельное время на дашборде: `study_sessions`
  получила `language`, `/api/stats` отдаёт `minutes_by_language`
  (kz/en, today/week/all_time) отдельно от общего.
- **2026-08-21** — грамматика: справочник сгруппирован по категориям
  (Падежи, Tenses, Articles и т.д. — уровень категории считается
  динамически), +24 новые темы (12 kz, 12 en — включая Past Perfect,
  Articles и Prepositions, ранее не покрытые вообще, согласно слабым
  местам из профиля), +120 упражнений (по 5 на новую тему). Итого 44
  темы, 220 дриллов.
- **2026-08-21** — компактизация: блок времени на дашборде (9 карточек
  → 1 таблица), справочник грамматики (категории сворачиваются,
  закрыты по умолчанию).
- **2026-08-21** — экзамен на уровень (вкладка «Экзамен» в Теории): до
  50 случайных вопросов из пула дриллов уровня, 100% для сдачи —
  частичный зачёт не считается. Пул растёт вместе с новыми темами/
  дриллами, отдельного банка вопросов нет. Результат — server-side
  (`level_exam_attempts`, полная история попыток).
- **2026-08-21** — раздел чтения (вкладка «Чтение» в Теории): тексты
  с уровнем/темой/стилем (`textbook` — плотно завязан на лексику
  колоды, `genre` — аутентичный жанровый регистр) и упражнениями на
  понимание после каждого текста. Весь контент оригинальный, без
  копирования внешних источников. Стартовый пакет: 4 текста (kz —
  семейная поездка A2 + сказка «Лиса и заяц» B1; en — рабочий день в
  маркетинговом агентстве B2 + статья про «цифровой детокс бренда»
  C1), растёт со временем как и остальной контент.
